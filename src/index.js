const aspects = require('./aspects')

const findRatio = function(name) {}

//
const beAspect = function(obj) {
  let ratio = findRatio(obj.aspect)
  if (obj.width) {

  }
  return obj
}
module.exports = beAspect
